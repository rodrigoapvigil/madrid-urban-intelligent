'use client';

import React from 'react';
import MadridLanding from './MadridLanding';

const MadridPage: React.FC = () => {
  return (
    <main style={{ minHeight: '100vh', backgroundColor: '#ffffff' }}>
      <MadridLanding />
    </main>
  );
};

export default MadridPage;
